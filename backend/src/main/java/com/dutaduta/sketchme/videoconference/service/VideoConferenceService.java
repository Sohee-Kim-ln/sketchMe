package com.dutaduta.sketchme.videoconference.service;

import com.dutaduta.sketchme.videoconference.dto.response.CreateSessionResponseDTO;
import com.dutaduta.sketchme.videoconference.dto.response.SearchSessionResponseDTO;
import org.springframework.stereotype.Service;

@Service
public class VideoConferenceService {
    public SearchSessionResponseDTO searchSession(String sessionId) {
    }

    public CreateSessionResponseDTO makeSession(String sessionId) {
    }
}
